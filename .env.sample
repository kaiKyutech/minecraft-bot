# ============================================================
# Minecraft Server Settings
# ============================================================
# サーバーのホストとポート
MC_HOST=localhost
MC_PORT=25566

# AI Botの名前（複数体の場合は自動的に連番が付く: Bot1, Bot2, ...）
MC_USERNAME=Bot

# Minecraftバージョン（空欄で自動検出）
MC_VERSION=

# ============================================================
# Observer Pool Settings (視覚システム)
# ============================================================
# Camera-Bot数（視覚専門ボット）
CAMERA_COUNT=1

# AI Bot数（GOAP実行ボット）
AI_BOT_COUNT=1

# Camera-Bot viewer開始ポート（Cam1: 3007, Cam2: 3008, ...）
CAMERA_START_PORT=3007

# ============================================================
# Debug Settings
# ============================================================
# デバッグログを有効化（1で有効、0または空欄で無効）
PLANNER_DEBUG=0
